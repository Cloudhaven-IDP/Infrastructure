service:
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 8080

configs:
  secret:
    argocdServerAdminPassword: "" # leave empty to use default auto-generated password

  cm:
    admin.enabled: true
    admin.apiKey: true
    application.instanceLabelKey: argocd.argoproj.io/instance
    exec.enabled: false
    server.rbac.log.enforce.enable: false
    timeout:
      hard.reconciliation: 0s
      reconciliation: 180s
